{
    "scripts": {
        "build:frontend": "cd frontend && npm run build",
        "build:backend": "cd backend && npm run build",
        "build": "npm run build:frontend && rm -rf backend/public && cp -r frontend/dist backend/public && npm run build:backend",
        "start": "cd backend && npm start"
    }
}
